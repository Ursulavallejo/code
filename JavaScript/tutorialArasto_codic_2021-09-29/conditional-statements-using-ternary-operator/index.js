// Other way of using conditional without if or else

/*condition always be followed by a ? after a block of code : after a block of code */

const canCharacterUseWands = false // run the second option as the first is false

canCharacterUseWands ? alert('You can use wands!'): alert('You cannot use wands!')